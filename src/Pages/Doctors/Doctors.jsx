import { Helmet } from "react-helmet-async";

const Doctors = () => {
   return (
      <div className="mt-24">
         <Helmet>
            <title>Health Care || Doctors</title>
         </Helmet>
         thsi is from doctors
      </div>
   );
};

export default Doctors;